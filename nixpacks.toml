[phases.setup]
nixPkgs = ["python39", "nodejs_20", "nodePackages.npm"]

[phases.build]
cmds = [
  "cd frontend && npm install && npm run build && cd ..",
  "pip install -r backend/requirements.txt"
]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
